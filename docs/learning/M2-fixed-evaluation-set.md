# M2 固定评测集：4.2 授信申请

## 评测目的

在生成 Embedding 之前，先验证 DOCX 解析和分片是否保留了后续检索所需的证据。每道题的期望证据范围在检索前固定，不能根据检索结果临时修改。

## 固定范围

- 文档：升恒消费金融接口文档 DOCX
- 章节：`授信申请`，对应原文中 `4.2.授信申请`
- 正文证据序号：86–93
- 请求字段表：94–127
- 返回参数：128–130
- 下一章节 `提现申请` 从 131 开始，不属于本评测范围

## 问题与期望证据

| 编号 | 问题 | 期望证据 | 验收重点 |
|---|---|---|---|
| Q01 | 授信申请接口用于什么业务？ | 86–88 | 找到接口用途正文 |
| Q02 | 发起授信申请前需要完成什么操作？ | 89–90 | 找到前置条件和 C132 |
| Q03 | 授信申请的请求地址是什么？ | 91–92 | 找到地址分片 |
| Q04 | `applyAmt` 的类型、必填性和含义是什么？ | 表格行 117 | 表头与当前行必须同时存在 |
| Q05 | `applyAmt` 的金额示例是什么？ | 表格行 117 | 示例不能被清洗掉 |
| Q06 | `billNo` 是否必填，代表什么？ | 表格行 95 | 必填标识和字段说明完整 |
| Q07 | 哪些字段是非必填字段？ | 表格行 104、106、119–121、125–127 | 不能把 N 误判为 Y |
| Q08 | 授信申请成功后返回哪个申请编号？ | 128–130 | 找到返回字段和说明 |
| Q09 | 文档是否声明了某个未出现的字段 `riskScore`？ | 无目标证据 | 无证据时必须返回证据不足 |
| Q10 | `applyAmt` 是否属于提现申请字段？ | 表格行 117；排除 131 之后内容 | 章节边界不能串入下一接口 |

## 评测规则

- 解析验收：目标证据分片存在、章节范围正确、来源序号可反查。
- 分片验收：表格问题的分片同时包含表头和当前行。
- 无证据验收：Q09 不得通过猜测返回答案。
- 后续检索验收：以这些期望证据为金标准，计算 Recall@K 和引用命中率。
