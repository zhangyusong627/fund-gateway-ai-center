# M2 分片长度统计

> 文档角色：M2 专题证据。阶段主总结见 [`M2-acceptance-2026-09-10.md`](M2-acceptance-2026-09-10.md)，本文件不单独代表阶段整体结论。

- 统计日期：2026-09-10
- 范围：`授信申请` 章节（解析元素 86–130）
- 分片数：38
- 最短：4 字符
- 中位数：63 字符
- P95：90 字符
- 最长：237 字符
- 平均：66.32 字符

## 决定

当前固定评测范围内，所有分片都很短，最长分片是接口用途、前置条件、请求地址和请求参数说明的连续正文，长度为 237 个字符。第一版不再做二次字符切分，也不增加重叠窗口：

- 普通正文按章节保留完整语义；
- 表格按“表头 + 当前行”保留字段上下文；
- 没有发现需要被截断的超长分片。

`chunk_size` 暂不作为硬编码字符常量写入代码。进入 Embedding 前继续以 Token 统计复核；若后续文档出现超长正文，再新增基于 Token 上限的二次切分策略。
