# M2 pgvector 离线索引与在线检索验证

> 文档角色：M2 专题证据。阶段主总结见 [`M2-acceptance-2026-09-10.md`](M2-acceptance-2026-09-10.md)，本文件不单独代表阶段整体结论。

- 执行日期：2026-09-10
- 集合：`m2_shengheng_credit_application_v1`
- 数据库：本地 Docker PostgreSQL 16 + pgvector
- Embedding：本地 `BAAI/bge-small-zh-v1.5`，512 维，L2 归一化
- 输入分片：`授信申请`章节 38 个

## 结果

- 集合注册成功，模型配置与向量维度一致。
- 38 个分片幂等写入 `knowledge_chunks`。
- 查询“授信申请需要填写申请金额”，Top-K=3 返回 3 条结果。
- 第一名是 `applyAmt` 表格证据块，余弦相似度约 `0.6722`。
- 第二名是授信申请接口说明和前置条件分片，约 `0.6203`。
- 第三名是 `billNo` 字段证据块，约 `0.5806`。
- 查询结果现在同时返回文档 ID、版本、章节、表格序号、行号和 locator；`applyAmt` 结果可定位为 `授信申请#117-117`。

## 这次验证证明了什么

文档解析 → 分片 → 本地 Embedding → pgvector 写入 → 查询向量化 → 余弦 Top-K 已经连通。集合配置、向量维度和归一化状态都被持久化校验。

## 当前限制

检索结果已经补齐来源引用字段；下一步需要把固定评测集逐题跑完，计算 Recall@K 和引用命中率。
