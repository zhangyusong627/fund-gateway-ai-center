# D1 复盘

首次调用已通过：应用在 JDK 21 下以 `d1` profile 启动，向 `deepseek-v4-flash` 发送一次同步请求，返回成功并记录 token 用量。请求中的提示和认证头未落盘；响应证据目录见同目录的 `D1-call-*`。

发现的坑：Maven 首次解析依赖耗时较长；工作目录在执行期间先后完成了合规改名和语义化改名，当前项目目录为 `fund-gateway-ai-center`。Embedding 不能凭文档类型直接拍板，表格与流程图要先结构化解析，再用混排合成样例评测。

下次调整：先建立表格/流程图解析的最小合成样例，再比较候选 Embedding 与关键词检索；不把“能调用”当作检索质量证明。
