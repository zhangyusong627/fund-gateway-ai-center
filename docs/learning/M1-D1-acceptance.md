# M1-D1 阶段验收记录

## 业务目标

在合成资方接口场景中，完成一次只读诊断：模型读取契约、运行指标和历史故障，Java 执行确定性规则与最终输出门禁，系统只生成建议，不执行配置修改。

## 三层验收

### 已运行

- 三个只读工具真实完成调用和结果回灌。
- DeepSeek `deepseek-v4-flash` 真实调用成功。
- Java 规则基线生成并保存。
- 最终模型 JSON 映射成功。
- 规则 ID 白名单和中文业务文本门禁通过。
- 完整证据见 `M1-real-call-1788930045785/`。
- `mvn -B test` 通过。

### 已理解

- 模型只能提出工具调用，Java 白名单决定是否执行。
- 工具原始 JSON 先映射为证据对象，再交给确定性规则服务。
- 模型最终报告与 Java 规则基线并存，模型不能覆盖规则判断。
- 结构、规则枚举和中文业务字段任一不合格，门禁都会停止流程。

### 已独立验证

- 自定义规则名被 `ModelDiagnosisGate` 拒绝。
- 缺少中文业务文本会被语言门禁拒绝。
- 合法 JSON 报告可映射为 `ModelDiagnosisReport` 并通过门禁。
- 未注册工具名会被工具注册表拒绝。
- 端到端真实调用在独立 profile 下可复现。

## 范围边界

- 只读诊断和建议生成。
- 不修改限流、超时或线程池配置。
- 不执行审批、恢复、切换或生产操作。
- `reasoning_content` 只保存为原始证据，不作为业务结论。

## 结论

M1-D1 三层验收通过，下一步进入核心代码按时序讲解和 M1-D2 任务规划。
