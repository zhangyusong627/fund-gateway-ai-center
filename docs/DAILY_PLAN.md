# 每日执行计划

本文件把路线图拆成每天可执行、可验收的工作。项目任务、Java 面试训练、AI 技术学习和复盘并行推进；完成代码不等于完成学习认证。

状态按 `docs/EXECUTION_PROTOCOL.md` 记录；没有证据的项目或面试任务不得标记为完成。

## 2026-09-07

### 项目：D1 收尾与 D2 预告

状态：D1 阶段认证已完成；D2 安排在 2026-09-08，今天不执行 D2。

- 确认 D1 认证记录和当前项目状态。
- 为明天 D2 准备三个样例的验收边界，不实现代码。

验收产物：今日任务核对记录；D2 三个合成样例和阶段认证安排在 2026-09-08。

### Java 面试训练：1.5 小时

- 30 分钟：闭卷复习当前重测题，优先集合、并发和 JVM 薄弱点。
- 30 分钟：一次口头模拟，只回答一道题，先完整作答再反馈。
- 20 分钟：复述一个 Java 后端项目设计或故障排查案例。
- 10 分钟：记录错题、表达问题和次日复习点。

### 当日复盘

- 今天真正能独立解释什么？
- 哪些内容只是看过代码或听过讲解？
- 哪一个具体问题需要调整明天的学习方式？

### 今日实际结果

- 项目：D1 认证已完成；D2 未开始，按计划留到明天。
- 面试：已完成一次闭卷复习、一次口头模拟和一次故障排查复述；闭卷题 6/10，口头题 8/10，故障复述 8/10。
- 面试薄弱点：继续巩固 HashMap 并发扩容风险、ConcurrentHashMap 可见性与扩容协作；复述故障时准确使用 Hippo4j 组件名称。
- AI 技术学习：今天未安排新的 AI 概念学习，保持未完成。
- 当日复盘：已能独立解释 HashMap 并发不安全的主要原因、ConcurrentHashMap 的 CAS 加桶头锁机制，以及请求头过大故障的根因与修复边界；部分细节仍依赖提示，需要明日先做延迟复习。

## 每日记录模板

```markdown
## YYYY-MM-DD

### 项目任务
- 目标：
- 输出物：
- 验收结果：

### Java 面试训练
- 闭卷题：
- 口头题：
- 薄弱点：

### AI 技术学习
- 今日概念：
- 能否独立解释：

### 复盘
- 已掌握：
- 仅看过：
- 明日调整：

## 2026-09-08

### 项目：M0-D2 结构化输出实验

状态：明日执行；范围只限结构化映射、结构校验和业务校验，不进入工具循环或代码生成。

#### 开工任务卡

- 业务目标：把模型返回的 JSON 转成有明确字段的 Java 结果，并证明格式正确不等于业务有效。
- 输入 / 输出：三个合成 JSON 样例；输出为结构化 Java 对象、校验结果和可审计错误信息。
- 状态与异常分支：合法样例通过；缺字段样例在结构校验阶段失败；格式合法但业务非法样例在业务校验阶段失败。
- 约束：不写真实机构数据，不把提示词或密钥写入代码、文档或日志，不改依赖版本，不提前实现 M4 代码生成链路。
- 正常预期 / 失败预期：正常样例能映射并通过两层校验；失败样例必须明确失败层级和字段/规则。
- 不在本次范围内：工具调用循环、向量检索、数据库、契约发布和代码生成。
- 验收方式：Java 测试覆盖三个样例；保存脱敏输入、结构化结果和校验结果；完成“已运行 / 已理解 / 已独立验证”三层记录。

#### 明日执行顺序

1. 先定义合成结果模型和三条业务规则，写清每条规则属于结构校验还是业务校验。
2. 实现 JSON 到 Java 对象的映射，以及结构校验和业务校验的分层入口。
3. 用三个样例运行测试，确认失败不会被模型再次猜测或静默修正。
4. 保存最小脱敏证据，解释 Spring AI 结构化输出与确定性校验的边界。
5. 完成阶段自测：能独立说明三种样例分别在哪一层通过或失败。

### Java 面试训练：1.5 小时

- 30 分钟：延迟复习 HashMap 并发扩容、ConcurrentHashMap 可见性与扩容协作。
- 30 分钟：一次口头模拟，只回答一道题。
- 20 分钟：复述 Hippo4j 请求头过大故障，准确说出 QueryString 与 POST Body 的关系。
- 10 分钟：记录评分、错点和 D2 学习迁移点。

### 当日复盘

- 项目：分别记录结构化映射、结构校验、业务校验是否已运行、已理解、已独立验证。
- 面试：记录延迟复习是否能脱离提示完成。
- 明日唯一收口条件：三个样例的分层结果和测试证据齐全后，才进入 D3 规划。

## 2026-09-07 D2 提前执行记录

- 项目：已按用户确认提前执行 D2；升恒授信申请结构化映射与三层校验实现完成。
- 验收：`fund-knowledge` 测试 4/4 通过；证据见 `docs/learning/D2-structured-output-2026-09-07.md`。
- 未完成：学习者独立口头解释和全仓库 clean verify，需在本次收工前补齐。
- D2 真实联调：已调用 1 次；模型返回可解析且业务字段通过，但未满足合成数据约束，已被 Java 门禁拒绝。
```
